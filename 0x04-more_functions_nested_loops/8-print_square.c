#include "main.h"

/**
 * print_square - prints square
 * @n : num of _ to be printed
 * Return: 0
 */
void print_square(int n)
{
	int i = 0, c;

	while (i < n && n > 0)
	{
		ii = 0;
		while (c < n)
		{
			_putchar('#');
			c++;
		}
		_putchar('\n');
		i++;
	}
	if (i == 0)
		_putchar('\n');
}
